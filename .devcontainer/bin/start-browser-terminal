#!/usr/bin/env bash
set -euo pipefail

cd "${CODESPACE_VSCODE_FOLDER:-/workspaces/gadk-sandbox}"

exec ttyd \
  --interface 0.0.0.0 \
  --port "${BROWSER_TERMINAL_PORT:-7681}" \
  --writable \
  bash -c 'export PATH="${GADK_VENV:-/opt/gadk-venv}/bin:${PATH}"; exec bash -i'
